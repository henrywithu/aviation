import fs from 'node:fs';import {parse} from '@babel/parser';const ast=parse(fs.readFileSync('research/modules/28866.js','utf8'));let mode='',data={privacy:[],terms:[]};function walk(n){if(!n||typeof n!=='object')return;if(n.type==='ObjectProperty'&&n.key.name==='title'&&n.value.type==='StringLiteral'){mode=n.value.value==='Privacy Policy'?'privacy':'terms'}if(n.type==='ObjectProperty'&&n.key.name==='children'&&n.value.type==='StringLiteral'&&n.value.value.length>70&&mode)data[mode].push(n.value.value);for(const[k,v]of Object.entries(n)){if(k==='loc')continue;if(Array.isArray(v))v.forEach(walk);else if(v&&typeof v==='object')walk(v)}}walk(ast);fs.writeFileSync('src/data/legal.json',JSON.stringify(data,null,2));
